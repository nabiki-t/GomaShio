<ContentDialog
    x:Uid="PasswordDialog"
    x:Class="GomaShio.PasswordDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:GomaShio"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Password"
    PrimaryButtonText="OK"
    SecondaryButtonText="Cancel"
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick" Loaded="ContentDialog_Loaded">

    <Grid HorizontalAlignment="Stretch" Margin="0,0,-0.286,-16" VerticalAlignment="Stretch">
        <TextBox Name="PasswordText" Margin="10,33,0,0" Text="TextBox" VerticalAlignment="Top" Height="32" HorizontalAlignment="Stretch"/>
        <CheckBox x:Uid="SavePasswordCheck" Name="SavePasswordCheck" HorizontalAlignment="Stretch" Margin="10,70,0,0" VerticalAlignment="Top" Height="43" Content="Save Password" />
        <TextBlock x:Uid="PasswordErrorText" Name="PasswordErrorText" HorizontalAlignment="Stretch" Margin="10,6,21,0" Text="Password unmatch" TextWrapping="Wrap" VerticalAlignment="Top" Foreground="Red"/>
    </Grid>
</ContentDialog>
